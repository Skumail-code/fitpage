var apiUrl = 'http://coding-assignment.bombayrunning.com/data.json';

  // if (criteria['type'] == 'variable') ...[
              //   Text('Variable Text: ${criteria['text']}'),

              //   // Display variable details based on the type
              //   for (var entry in criteria['variable'].entries) ...[
              //     Text('Variable: ${entry.key}'),
              //     if (entry.value['type'] == 'value')
              //       Text('Values: ${entry.value['values']}'),
              //     if (entry.value['type'] == 'indicator') ...[
              //       Text('Study Type: ${entry.value['study_type']}'),
              //       Text('Parameter Name: ${entry.value['parameter_name']}'),
              //       Text('Min Value: ${entry.value['min_value']}'),
              //       Text('Max Value: ${entry.value['max_value']}'),
              //       Text('Default Value: ${entry.value['default_value']}'),
              //     ],
              //   ],
              // ],